export function verifyToken(req, res, next) {
  // Placeholder — replace with your real auth logic
  next();
}

export function isAdmin(req, res, next) {
  // Placeholder — allow all for now
  next();
}
